## SEO & i18n quick tips
- Update `<title>` and meta in `src/index.html`.
- Consider adding alternate language routes (e.g., `/en`) if needed.

## Custom domain on GitHub Pages
- Add a `CNAME` file at repo root with your domain, and set it in Settings → Pages.
